# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env — secrets stay out
.env
.env.*.local

# Claude Code personal overrides (auto-created; never shared)
.claude/settings.local.json

# The compiled design bundle: 7.6 MB, ~5.6 MB of it base64 fonts.
# The readable extract is committed at design-reference/Pinstory-source-readable.html.
design-reference/Pinstory.html

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- REMOVED from the previous .gitignore -----------------------------------
# The AI Workspace is part of the project architecture and must evolve with the
# codebase. These are now tracked:
#   .claude/                      CLAUDE.md
#   FEATURES_SCOPE.md             FRONTEND_INSTRUCTIONS.md
#   BACKEND_INSTRUCTIONS.md       TESTING_PLAN.md
#   DEPLOYMENT.md                 design-reference/
#   ai-knowledge-base/            (was untracked entirely)
# ----------------------------------------------------------------------------

# Personal / temporary workspace artifacts
PINSTORY_PROJECT_SUMMARY_PROMPT.md
PROJECT_AUDIT_AND_RECOMMENDATIONS.md
gitignore.patched
